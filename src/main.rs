use aoc::{day17, get_day, pretty};

fn main() {
    //let input = &get_day(16);
    let input = day17::INPUT;
    //pretty(&day17::parse(input));
    dbg!(day17::part_one(input));
    //dbg!(day16::part_two(input));
}
